<%= form_for @nameserver, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label "Endereço:", :class => 'control-label' %>
    <div class="controls">
      <%= f.dns_field :address, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label "Nome:", :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'text_field' %>
    </div>
  </div>
    <div class="control-group" >
      <%= f.label "Vip:", :class => 'control-label' %>
      <div class="controls" >
        <%= f.check_box :vip, {:class => 'check_box'}%>
      </div>
    </div>
  <div class="control-group">
    <%= f.label "Primary:", :class => 'control-label' %>
    <div class="controls">
      <%= f.check_box :primary, :class => 'check_box' %>
    </div>
  </div>

  <div class="control-group">
    <%= f.label "Internal:", :class => 'control-label' %>
    <div class="controls">
      <%= f.check_box :internal, :class => 'check_box' %>
    </div>
  </div>

  <div class="control-group">
    <div class="controls">
    <%= f.submit nil, :class => 'btn btn-primary btn-large' %>
    <%= link_to t('.cancel', :default => "Cancelar"),
                nameservers_path, :class => 'btn btn-inverse btn-large ' %>
    </div>
  </div>
<% end %>
