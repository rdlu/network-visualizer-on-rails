<h2>Perfis de Teste</h2>
<div class="row-fluid">
  <div class="span12">
    <% if can? :create, TestProfile %>
        <div class="navbar">
          <div class="navbar-inner">
            <div class="span2 offset10">
              <%= link_to "Novo Perfil de Teste", new_test_profile_path, :class => "btn btn-primary btn-block" %>
            </div>
          </div>
        </div>
    <% end %>
    <div class="alert alert-warning">
      Os perfis de teste devem ser planejados com cautela antes de qualquer cadastramento ou modificação.<br>
      Eles podem mudar todo o comportamento dos testes!
    </div>
  </div>
</div>
<div class="row-fluid">
  <div class="span12">
    <table class="table table-bordered table-hover">
      <tr>
        <th>Nome</th>
        <th>Método de configuração</th>
        <th colspan="3"></th>
      </tr>

      <% @test_profiles.each do |test_profile| %>
          <tr>
            <td><%= test_profile.name %></td>
            <td><%= test_profile.config_method %></td>
            <td><%= link_to 'Show', test_profile %></td>
            <td><%= link_to 'Edit', edit_test_profile_path(test_profile) %></td>
            <td><%= link_to 'Destroy', test_profile, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
      <% end %>
    </table>
  </div>
</div>
